# CustomThread
---  
## 📋 Описание проекта
Пользовательский пул потоков `CustomThreadExecutor`, реализующий интерфейс `CustomExecutor`:
```java
interface CustomExecutor extends Executor {
    void execute (Runnable command);
    <T> Future<T> submit (Callable<T> callable);
    void shutdown ();
    void shutdownNow ();
}
```
с настраиваемым управлением очередями, логированием, параметрами и политикой отказа.
#### Основные параметры настройки пула потоков

| Параметр            | Описание                                                                |
|---------------------|-------------------------------------------------------------------------|
| **corePoolSize**    | минимальное (базовое) количество потоков                                | —                               |
| **maxPoolSize**     | максимальное количество потоков                                         |
| **keepAliveTime**   | период время, в течение которого поток может простаивать до своего завершения |
| **timeUnit**        | единица измерения этого периода времени                                 |
| **queueSize**       | ограничение на количество задач в очереди                                                                   |
| **minSpareThreads** |минимальное число «резервных» потоков, которые должны быть всегда доступны. Если число свободных потоков падает ниже этого значения, пул должен создавать новые потоки, даже при невысокой нагрузке.                                                                    |

## Ключевые особенности
* Отдельная очередь задач для каждого рабочего потока
* Поддержка резервных потоков `minSpareThreads`
* Пользовательская фабрика рабочих потоков с логированием
* Балансировка нагрузки по алгоритму `Round Robin`
* Подробное логирование на базе логгера Slf4j и Logback
* Обработка отказов при переполнении очереди - выброс RejectedExecutionException  

При разработке пула потоков реализованы:
* шаблон проектирования `Наблюдатель` с уведомлением наблюдателя в отдельных потоках `NotificationThreadExecutor`
* шаблон проектирования `Стратегия` для обеспечения сбора статистики по выполняемым задачам и сохранения универсальности пользовательского пула потока  
* шаблон проектирования `Фабрика` при установке класса пула потоков для обработки задач 

## ⚙️ Технологии
* Java 21
* Maven
* SLF4J API + Logback

## 🛠 Установка и запуск

1. Склонировать репозиторий:

   ```bash
   git clone https://github.com/idAndrey/CustomThread.git
   cd CustomThread
   ```
2. Собрать проект:

   ```bash
   mvn clean install
   ```
3. Настроить значения параметров пользовательского пула в файле `executor.properties`
4. Задать параметры демонстрационной задачи в файле `jobs.json`
5. Запустить программу:  

   ```bash
   java -jar target/custom-thread-1.0-SNAPSHOT.jar [CUSTOM|STANDARD]
   ```
Для использования стандартного пула потоков передайте в функцию `main` аргумент `STANDARD`.  
Для применения пользовательского пула потоков укажите аргумент `CUSTOM`.  
В случае отсутствия аргументов будет установлен стандартный пул потоков.

## 🔍 Демонстрация работы
Параметры тестовой задачи указаны в файле `jobs.json`:

| Параметр         | Описание                   | Значение         |
|------------------|----------------------------|------------------|
| **jobName**      | имя задания                | DemonstrationJob |                             |
| **jobId**        | номер задания              | 549              |
| **taskCount**    | количество задач в задании | 1000             |
| **duration**     | длительность одной задачи  | 15               |
| **interval**     | интервал создания задачи   | 1                |
| **timeUnit**     | единица измерения времени  | MILLISECONDS     |
| **executorType** | тип пула потоков           | CUSTOM           |

Задачи в задании `jobId 549` в количестве 1000 задач длительностью 15 мс каждая отправляются на выполнение с интервалом 1 мс.  
В настоящий момент в отладочных целях выполняется только первое задание из списка заданий в файле `jobs.json`. 
### Пользовательский пул потоков СUSTOM

В случае пользовательского пула потоков `CUSTOM` каждый поток имеет индивидуальную очередь.  
Параметры настройки пула заданы в файле `executor.properties`  
При значении размера очереди рабочего потока `queueSize = 40` отклонённые задачи отсутствуют.
1000 задач выполняются за 1987 мс.

### Стандартный пул потоков STANDARD
В случае использования стандартного пула потоков `STANDARD` на базе ThreadPoolExecutor с аналогичными параметрами следует определить размер очереди задач, так как у рабочего потока индивидуальная очередь отсутствует и задачи поступают в общую очередь.  
Каждый поток обрабатывает `1 / duration` задач в миллисекунду.  
Задачи поступают со скоростью:
```
arrivalRate = 1 / interval = 1.0 задач/мс.  
```
Все потоки вместе обрабатывают очередь задач со скоростью `totalThroughput`:  
```
totalThroughput = maxPoolSize * (1/duration) ≈ 0.533 задач/мс
```
Скорость увеличения очереди `queueGrowthRate`:
```
queueGrowthRate = arrivalRate - totalThroughput  
queueGrowthRate = 1.0 - 0.533 ≈ 0.467 задач/мс
```
Общая длительность задания `totalTime`:
```
totalTime = taskCount * interval = 1000 * 1 = 1000 мс.
```
Очередь растёт с `queueGrowthRate` в течение `totalTime`.
Следовательно, минимальный размер общей очереди `minQueueSize` для того, чтобы все задачи были выполнены без потерь:  
```
minQueueSize = queueGrowthRate × totalTime = 0.467 × 1000 = 467 задач
```
Итоговая формула:

```
minQueueSize = ((1/interval) - maxPoolSize/duration) * taskCount * interval

minQueueSize = 467
```

При данном значении параметра пула потоков `queueSize`, равном вычисленному значению 467 задач, существенная часть задач не помещается в очередь и отбрасывается, что можно объяснить неучтённым временем на переключение контекста потоков и потерями на работу с очередью, на постановку задачи в очередь и на её удаление из очереди.  
Как показал опыт, при размере очереди, равном 700 задач, все задачи обрабатываются без потерь.
При этом общее время их обработки составляет 3985 мс, что существенно больше, чем при использовании пользовательского пула потоков `CUSTOM`.

### Сравнение работы пулов CUSTOM и STANDARD

| Параметр                                        | CUSTOM          | STANDARD |
|-------------------------------------------------|-----------------|----------|
| **количество задач**                            | 1000            | 1000     |
| **количество отклонённых задач**                | 0               |          |
| **общее время выполнения**                      | 2010 мс         | 3985 мс  |
| **параметр `queueSize`**                        | 40 (40*8 = 320) | 700      |
| **расчётный размер очереди**                    | 59              | 467      |
| **максимальный фактический<br/>размер очереди** | 249             | 692      |

Пользовательский пул значительно быстрее обрабатывает поток частых задач (период поступления которых на порядок меньше их длительности).  
Стандартный пул потоков с одной общей очередью требует значительный размер очереди для выполнения всех поступающихзадач без их отклонения.  
Фактический размер очереди стандартного пула потоков существенно превышает расчётное значение размера очереди, что объяснимо тем, что в расчёте не учтено время на переключение контекста потоков и время для выполнения операций над очередью в потокобезопасной реализации, когда очередь может блокироваться каким-либо потоком.  

## Дальнейшая доработка
* реализация класса задачи RandomTask со случайной длительностью выполнения
* реализация класса задания JobSeries для комплексного исследования работы пользовательского пула потоков `CUSTOM`
* запуск на выполнение большого количества задач со случайным значением длительности выполнения на пользовательском пуле потоков и стандартном пуле потоков

## Алгоритм Round Robin
В данном алгоритме балансировки потоков предполагается,что все задачи имеют одинаковый приоритет. Задачи последовательно назначаются в каждый активный поток пула потоков. При достижении последнего потока следующая задача назначается в первый активный поток и так далее. Алгоритм `Round Robin`отличается простотой реализации и равномерным распределением задач между потоками, когда каждый поток получает равное количество задач.  
К недостаткам алгоритма `Round Robin` относится то, что не учитывается загруженность того или иного потока в составе пула потоков.  

## Поддержка
Если у вас возникли вопросы по проекту или сложности при его запуске, создайте
[обсуждение](https://github.com/idAndrey/issues/new/choose) в данном репозитории или свяжитесь с разработчиком по электронной почте <a href="mailto:mail@example.com"><ermakov.andrey@ya.ru></a> или телеграм [Андрей Ермаков](https://t.me/pc022979700).  
